# 项目级协作规则

## 前端交互视觉

- 全局交互控件不得使用浏览器 `outline`、Tailwind `ring`、`box-shadow` 或高对比度边框作为 focus/focus-visible 高亮框。
- 选中态使用背景色、文字色或图标表达；禁用态使用统一灰态表达。键盘焦点如需额外可见反馈，只能使用不形成外框的背景、文字或图标变化。
- 新增或修改组件时不得引入 `focus:ring-*`、`focus-visible:ring-*`、`focus-within:ring-*`、`has-focus:ring-*`、`shadow-focus` 等高亮框样式。错误校验的语义色边框不属于 focus 高亮框，可以保留。
- 上述规则必须在全局样式入口兜底，不能依赖逐个页面或逐个组件自行处理。
- 直接写入系统剪贴板的复制按钮只显示一个复制图标，成功态可切换为单个完成图标；按钮文字必须放入 Tooltip 和 `aria-label`，不得在按钮内重复显示。菜单命令及“复制/克隆业务资源”不受此限制。

## 文件长度与模块组织

- 除生成代码和测试代码外，新建生产实现文件不得超过 500 行。生产源码中的 `#[cfg(test)]`、`mod tests` 等测试块不计入该上限。
- 已有生产文件超过 500 行时，不得继续向其中堆叠新的独立职责。若当前任务实质修改了超限文件，应在同一任务中把本次涉及的内聚逻辑拆出；仅有极小且孤立的修复时，不要求顺带重构无关历史代码，但不得让文件结构进一步恶化。
- 拆分必须按职责、领域概念或执行阶段进行，不得为了满足行数要求机械切割，也不得通过压缩格式、合并语句或删除必要注释规避上限。
- 同一功能、领域概念或模块需要两个及以上实现文件时，必须建立以该功能或模块命名的专属目录，并把相关文件集中放置；不得用一组散落在父目录中的同前缀文件代替目录。
- Rust 模块可根据现有结构使用 `feature.rs` + `feature/*.rs` 或 `feature/mod.rs` + 子文件；无论采用哪种形式，相关实现都必须归入同一模块目录，并保持 `api`、`app`、`domain`、`infra` 等分层边界。
- 不得把拆出的代码堆入职责不清的 `utils`、`helpers`、`common`、`misc` 等杂物文件或目录；共享代码必须有明确归属和稳定语义。
- 生成代码是指带有 `@generated`、`DO NOT EDIT` 等明确标记，或由项目生成流程写入的文件；测试代码包括独立测试文件、测试目录及生产源码内的测试模块。除此之外不豁免。

## Rust 后端验证频率

- 涉及 Rust 后端修改时，先完成当前任务范围内的全部代码变更，再统一进行验证；不得每完成一个小改动就运行一次验证。
- 实现过程中不得反复运行 `cargo test`、`cargo clippy`、`cargo build`、`cargo check` 或对应的 `make` 目标。只有遇到阻塞后续实现的编译问题时，才可执行一次范围尽可能小的诊断命令。
- 全部后端修改完成后只执行一轮收尾验证。`cargo test` 或 `cargo clippy` 已经触发编译时，不得再额外运行功能重叠的 `cargo build` 或 `cargo check`。
- 一轮验证通过后不得重复执行。若验证失败，应先集中修复已发现的问题，之后只重跑失败项，不得重新运行整套已通过的检查。
- 不得在同一后端任务中多次询问用户是否运行构建或验证；默认按以上规则在收尾阶段执行一次。仅当命令会产生外部副作用、需要额外权限，或预计资源消耗明显超出常规时，才询问一次。
